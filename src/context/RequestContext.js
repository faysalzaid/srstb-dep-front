import { createContext } from "react";

export const FuelContext = createContext(null);

export const MoneyContext = createContext(null);

export const ContractContext = createContext(null);

export const CarRentalContext = createContext(null);

export const EquipmentRentalContext = createContext(null);
